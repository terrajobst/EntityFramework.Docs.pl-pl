---
title: Ocena klienta a serwer — EF Core
author: smitpatel
ms.date: 10/03/2019
ms.assetid: 8b6697cc-7067-4dc2-8007-85d80503d123
uid: core/querying/client-eval
ms.openlocfilehash: e01bd146c4dfe7a8d36b641cb52ae366fddd8239
ms.sourcegitcommit: 9b562663679854c37c05fca13d93e180213fb4aa
ms.translationtype: MT
ms.contentlocale: pl-PL
ms.lasthandoff: 04/07/2020
ms.locfileid: "78417761"
---
# <a name="client-vs-server-evaluation"></a><span data-ttu-id="cd687-102">Ocena klienta a serwer</span><span class="sxs-lookup"><span data-stu-id="cd687-102">Client vs. Server Evaluation</span></span>

<span data-ttu-id="cd687-103">Zgodnie z ogólną zasadą Entity Framework Core próbuje ocenić kwerendę na serwerze w jak największej ilości.</span><span class="sxs-lookup"><span data-stu-id="cd687-103">As a general rule, Entity Framework Core attempts to evaluate a query on the server as much as possible.</span></span> <span data-ttu-id="cd687-104">EF Core konwertuje części kwerendy na parametry, które można ocenić po stronie klienta.</span><span class="sxs-lookup"><span data-stu-id="cd687-104">EF Core converts parts of the query into parameters, which it can evaluate on the client side.</span></span> <span data-ttu-id="cd687-105">Pozostała część kwerendy (wraz z wygenerowanymi parametrami) jest podana do dostawcy bazy danych w celu określenia równoważnej kwerendy bazy danych do oceny na serwerze.</span><span class="sxs-lookup"><span data-stu-id="cd687-105">The rest of the query (along with the generated parameters) is given to the database provider to determine the equivalent database query to evaluate on the server.</span></span> <span data-ttu-id="cd687-106">EF Core obsługuje częściową ocenę klienta w projekcji najwyższego poziomu (zasadniczo ostatnie wywołanie `Select()`).</span><span class="sxs-lookup"><span data-stu-id="cd687-106">EF Core supports partial client evaluation in the top-level projection (essentially, the last call to `Select()`).</span></span> <span data-ttu-id="cd687-107">Jeśli projekcji najwyższego poziomu w kwerendzie nie można przetłumaczyć na serwer, EF Core pobierze wszystkie wymagane dane z serwera i oceni pozostałe części kwerendy na kliencie.</span><span class="sxs-lookup"><span data-stu-id="cd687-107">If the top-level projection in the query can't be translated to the server, EF Core will fetch any required data from the server and evaluate remaining parts of the query on the client.</span></span> <span data-ttu-id="cd687-108">Jeśli EF Core wykrywa wyrażenie, w dowolnym miejscu innym niż projekcji najwyższego poziomu, które nie mogą być tłumaczone na serwer, a następnie zgłasza wyjątek środowiska wykonawczego.</span><span class="sxs-lookup"><span data-stu-id="cd687-108">If EF Core detects an expression, in any place other than the top-level projection, which can't be translated to the server, then it throws a runtime exception.</span></span> <span data-ttu-id="cd687-109">Zobacz, [jak działa kwerenda,](xref:core/querying/how-query-works) aby zrozumieć, jak EF Core określa, czego nie można przetłumaczyć na serwer.</span><span class="sxs-lookup"><span data-stu-id="cd687-109">See [how query works](xref:core/querying/how-query-works) to understand how EF Core determines what can't be translated to server.</span></span>

> [!NOTE]
> <span data-ttu-id="cd687-110">Przed wersją 3.0, Entity Framework Core obsługiwane oceny klienta w dowolnym miejscu w kwerendzie.</span><span class="sxs-lookup"><span data-stu-id="cd687-110">Prior to version 3.0, Entity Framework Core supported client evaluation anywhere in the query.</span></span> <span data-ttu-id="cd687-111">Aby uzyskać więcej informacji, zobacz [sekcję Poprzednie wersje](#previous-versions).</span><span class="sxs-lookup"><span data-stu-id="cd687-111">For more information, see the [previous versions section](#previous-versions).</span></span>

> [!TIP]
> <span data-ttu-id="cd687-112">Możesz wyświetlić [ten](https://github.com/dotnet/EntityFramework.Docs/tree/master/samples/core/Querying) przykład artykułu na GitHub.</span><span class="sxs-lookup"><span data-stu-id="cd687-112">You can view this article's [sample](https://github.com/dotnet/EntityFramework.Docs/tree/master/samples/core/Querying) on GitHub.</span></span>

## <a name="client-evaluation-in-the-top-level-projection"></a><span data-ttu-id="cd687-113">Ocena klienta w projekcji najwyższego poziomu</span><span class="sxs-lookup"><span data-stu-id="cd687-113">Client evaluation in the top-level projection</span></span>

<span data-ttu-id="cd687-114">W poniższym przykładzie metoda pomocnika służy do standaryzacji adresów URL blogów, które są zwracane z bazy danych programu SQL Server.</span><span class="sxs-lookup"><span data-stu-id="cd687-114">In the following example, a helper method is used to standardize URLs for blogs, which are returned from a SQL Server database.</span></span> <span data-ttu-id="cd687-115">Ponieważ dostawca programu SQL Server nie ma wglądu w sposób implementacji tej metody, nie można przetłumaczyć jej na język SQL.</span><span class="sxs-lookup"><span data-stu-id="cd687-115">Since the SQL Server provider has no insight into how this method is implemented, it isn't possible to translate it into SQL.</span></span> <span data-ttu-id="cd687-116">Wszystkie inne aspekty kwerendy są oceniane w bazie `URL` danych, ale przekazywanie zwrócone za pośrednictwem tej metody odbywa się na kliencie.</span><span class="sxs-lookup"><span data-stu-id="cd687-116">All other aspects of the query are evaluated in the database, but passing the returned `URL` through this method is done on the client.</span></span>

[!code-csharp[Main](../../../samples/core/Querying/ClientEval/Sample.cs#ClientProjection)]

[!code-csharp[Main](../../../samples/core/Querying/ClientEval/Sample.cs#ClientMethod)]

## <a name="unsupported-client-evaluation"></a><span data-ttu-id="cd687-117">Nieobsługiwała ocena klienta</span><span class="sxs-lookup"><span data-stu-id="cd687-117">Unsupported client evaluation</span></span>

<span data-ttu-id="cd687-118">Chociaż ocena klienta jest przydatna, może to czasami spowodować niską wydajność.</span><span class="sxs-lookup"><span data-stu-id="cd687-118">While client evaluation is useful, it can result in poor performance sometimes.</span></span> <span data-ttu-id="cd687-119">Należy wziąć pod uwagę następujące zapytanie, w którym metoda pomocnika jest teraz używana w filtrze where.</span><span class="sxs-lookup"><span data-stu-id="cd687-119">Consider the following query, in which the helper method is now used in a where filter.</span></span> <span data-ttu-id="cd687-120">Ponieważ filtr nie można zastosować w bazie danych, wszystkie dane muszą być pobierane do pamięci, aby zastosować filtr na kliencie.</span><span class="sxs-lookup"><span data-stu-id="cd687-120">Because the filter can't be applied in the database, all the data needs to be pulled into memory to apply the filter on the client.</span></span> <span data-ttu-id="cd687-121">Na podstawie filtru i ilości danych na serwerze ocena klienta może spowodować niską wydajność.</span><span class="sxs-lookup"><span data-stu-id="cd687-121">Based on the filter and the amount of data on the server, client evaluation could result in poor performance.</span></span> <span data-ttu-id="cd687-122">Tak Entity Framework Core blokuje taką ocenę klienta i zgłasza wyjątek środowiska uruchomieniowego.</span><span class="sxs-lookup"><span data-stu-id="cd687-122">So Entity Framework Core blocks such client evaluation and throws a runtime exception.</span></span>

[!code-csharp[Main](../../../samples/core/Querying/ClientEval/Sample.cs#ClientWhere)]

## <a name="explicit-client-evaluation"></a><span data-ttu-id="cd687-123">Jawna ocena klienta</span><span class="sxs-lookup"><span data-stu-id="cd687-123">Explicit client evaluation</span></span>

<span data-ttu-id="cd687-124">W niektórych przypadkach, takich jak:</span><span class="sxs-lookup"><span data-stu-id="cd687-124">You may need to force into client evaluation explicitly in certain cases like following</span></span>

- <span data-ttu-id="cd687-125">Ilość danych jest niewielka, dzięki czemu ocena na kliencie nie wiąże się z ogromną karą za wydajność.</span><span class="sxs-lookup"><span data-stu-id="cd687-125">The amount of data is small so that evaluating on the client doesn't incur a huge performance penalty.</span></span>
- <span data-ttu-id="cd687-126">Używany operator LINQ nie ma tłumaczenia po stronie serwera.</span><span class="sxs-lookup"><span data-stu-id="cd687-126">The LINQ operator being used has no server-side translation.</span></span>

<span data-ttu-id="cd687-127">W takich przypadkach można jawnie zdecydować się na `AsEnumerable` `ToList` ocenę`AsAsyncEnumerable` `ToListAsync` klienta, wywołując metody takie jak lub (lub asynchronicznie).</span><span class="sxs-lookup"><span data-stu-id="cd687-127">In such cases, you can explicitly opt into client evaluation by calling methods like `AsEnumerable` or `ToList` (`AsAsyncEnumerable` or `ToListAsync` for async).</span></span> <span data-ttu-id="cd687-128">Za `AsEnumerable` pomocą będzie przesyłania strumieniowego `ToList` wyników, ale przy użyciu spowodowałoby buforowanie przez utworzenie listy, która również zajmuje dodatkową pamięć.</span><span class="sxs-lookup"><span data-stu-id="cd687-128">By using `AsEnumerable` you would be streaming the results, but using `ToList` would cause buffering by creating a list, which also takes additional memory.</span></span> <span data-ttu-id="cd687-129">Chociaż jeśli wyliczasz wiele razy, przechowywanie wyników na liście pomaga więcej, ponieważ istnieje tylko jedno zapytanie do bazy danych.</span><span class="sxs-lookup"><span data-stu-id="cd687-129">Though if you're enumerating multiple times, then storing results in a list helps more since there's only one query to the database.</span></span> <span data-ttu-id="cd687-130">W zależności od konkretnego użycia należy ocenić, która metoda jest bardziej przydatna w przypadku.</span><span class="sxs-lookup"><span data-stu-id="cd687-130">Depending on the particular usage, you should evaluate which method is more useful for the case.</span></span>

[!code-csharp[Main](../../../samples/core/Querying/ClientEval/Sample.cs#ExplicitClientEval)]

## <a name="potential-memory-leak-in-client-evaluation"></a><span data-ttu-id="cd687-131">Potencjalny wyciek pamięci w ocenie klienta</span><span class="sxs-lookup"><span data-stu-id="cd687-131">Potential memory leak in client evaluation</span></span>

<span data-ttu-id="cd687-132">Ponieważ translacji kwerendy i kompilacji są kosztowne, EF Core buforuje skompilowany plan kwerend.</span><span class="sxs-lookup"><span data-stu-id="cd687-132">Since query translation and compilation are expensive, EF Core caches the compiled query plan.</span></span> <span data-ttu-id="cd687-133">Pełnomocnik buforowane może używać kodu klienta podczas wykonywania oceny klienta projekcji najwyższego poziomu.</span><span class="sxs-lookup"><span data-stu-id="cd687-133">The cached delegate may use client code while doing client evaluation of top-level projection.</span></span> <span data-ttu-id="cd687-134">EF Core generuje parametry dla części drzewa oceniane przez klienta i ponownie używa planu kwerend, zastępując wartości parametrów.</span><span class="sxs-lookup"><span data-stu-id="cd687-134">EF Core generates parameters for the client-evaluated parts of the tree and reuses the query plan by replacing the parameter values.</span></span> <span data-ttu-id="cd687-135">Ale niektórych stałych w drzewie wyrażeń nie można przekonwertować na parametry.</span><span class="sxs-lookup"><span data-stu-id="cd687-135">But certain constants in the expression tree can't be converted into parameters.</span></span> <span data-ttu-id="cd687-136">Jeśli pełnomocnik buforowane zawiera takie stałe, a następnie te obiekty nie mogą być zbierane śmieci, ponieważ są one nadal odwołuje.</span><span class="sxs-lookup"><span data-stu-id="cd687-136">If the cached delegate contains such constants, then those objects can't be garbage collected since they're still being referenced.</span></span> <span data-ttu-id="cd687-137">Jeśli taki obiekt zawiera DbContext lub innych usług w nim, a następnie może spowodować użycie pamięci aplikacji rośnie w czasie.</span><span class="sxs-lookup"><span data-stu-id="cd687-137">If such an object contains a DbContext or other services in it, then it could cause the memory usage of the app to grow over time.</span></span> <span data-ttu-id="cd687-138">To zachowanie jest zazwyczaj oznaką przecieku pamięci.</span><span class="sxs-lookup"><span data-stu-id="cd687-138">This behavior is generally a sign of a memory leak.</span></span> <span data-ttu-id="cd687-139">EF Core zgłasza wyjątek, gdy natknie się na stałe typu, który nie może być mapowany przy użyciu bieżącego dostawcy bazy danych.</span><span class="sxs-lookup"><span data-stu-id="cd687-139">EF Core throws an exception whenever it comes across constants of a type that can't be mapped using current database provider.</span></span> <span data-ttu-id="cd687-140">Typowe przyczyny i ich rozwiązania są następujące:</span><span class="sxs-lookup"><span data-stu-id="cd687-140">Common causes and their solutions are as follows:</span></span>

- <span data-ttu-id="cd687-141">**Przy użyciu metody wystąpienia:** Podczas korzystania z metod wystąpienia w projekcji klienta, drzewo wyrażeń zawiera stałą wystąpienia.</span><span class="sxs-lookup"><span data-stu-id="cd687-141">**Using an instance method**: When using instance methods in a client projection, the expression tree contains a constant of the instance.</span></span> <span data-ttu-id="cd687-142">Jeśli metoda nie używa żadnych danych z wystąpienia, należy rozważyć uczynienie metody statyczne.</span><span class="sxs-lookup"><span data-stu-id="cd687-142">If your method doesn't use any data from the instance, consider making the method static.</span></span> <span data-ttu-id="cd687-143">Jeśli potrzebujesz danych wystąpienia w treści metody, następnie przekazać określone dane jako argument do metody.</span><span class="sxs-lookup"><span data-stu-id="cd687-143">If you need instance data in the method body, then pass the specific data as an argument to the method.</span></span>
- <span data-ttu-id="cd687-144">**Przekazywanie stałych argumentów do metody:** Ten `this` przypadek występuje zazwyczaj przy użyciu w argument do metody klienta.</span><span class="sxs-lookup"><span data-stu-id="cd687-144">**Passing constant arguments to method**: This case arises generally by using `this` in an argument to client method.</span></span> <span data-ttu-id="cd687-145">Należy rozważyć podzielenie argumentu na wiele argumentów skalarnych, które mogą być mapowane przez dostawcę bazy danych.</span><span class="sxs-lookup"><span data-stu-id="cd687-145">Consider splitting the argument in to multiple scalar arguments, which can be mapped by the database provider.</span></span>
- <span data-ttu-id="cd687-146">**Inne stałe**: Jeśli stała jest natknąć się w każdym innym przypadku, a następnie można ocenić, czy stała jest potrzebna w przetwarzaniu.</span><span class="sxs-lookup"><span data-stu-id="cd687-146">**Other constants**: If a constant is come across in any other case, then you can evaluate whether the constant is needed in processing.</span></span> <span data-ttu-id="cd687-147">Jeśli jest to konieczne, aby mieć stałą lub jeśli nie można użyć rozwiązania z powyższych przypadków, a następnie utworzyć zmienną lokalną do przechowywania wartości i używać zmiennej lokalnej w kwerendzie.</span><span class="sxs-lookup"><span data-stu-id="cd687-147">If it's necessary to have the constant, or if you can't use a solution from the above cases, then create a local variable to store the value and use local variable in the query.</span></span> <span data-ttu-id="cd687-148">EF Core przekonwertuje zmienną lokalną na parametr.</span><span class="sxs-lookup"><span data-stu-id="cd687-148">EF Core will convert the local variable into a parameter.</span></span>

## <a name="previous-versions"></a><span data-ttu-id="cd687-149">Poprzednie wersje</span><span class="sxs-lookup"><span data-stu-id="cd687-149">Previous versions</span></span>

<span data-ttu-id="cd687-150">Poniższa sekcja dotyczy wersji EF Core przed 3.0.</span><span class="sxs-lookup"><span data-stu-id="cd687-150">The following section applies to EF Core versions before 3.0.</span></span>

<span data-ttu-id="cd687-151">Starsze wersje EF Core obsługiwane oceny klienta w dowolnej części kwerendy — nie tylko projekcji najwyższego poziomu.</span><span class="sxs-lookup"><span data-stu-id="cd687-151">Older EF Core versions supported client evaluation in any part of the query--not just the top-level projection.</span></span> <span data-ttu-id="cd687-152">Dlatego kwerendy podobne do tych zaksięgowanych w sekcji [Nieobsługiwany klient oceny](#unsupported-client-evaluation) działały poprawnie.</span><span class="sxs-lookup"><span data-stu-id="cd687-152">That's why queries similar to one posted under the [Unsupported client evaluation](#unsupported-client-evaluation) section worked correctly.</span></span> <span data-ttu-id="cd687-153">Ponieważ to zachowanie może spowodować niezauważone problemy z wydajnością, EF Core rejestrowane ostrzeżenie oceny klienta.</span><span class="sxs-lookup"><span data-stu-id="cd687-153">Since this behavior could cause unnoticed performance issues, EF Core logged a client evaluation warning.</span></span> <span data-ttu-id="cd687-154">Aby uzyskać więcej informacji na temat wyświetlania danych wyjściowych rejestrowania, zobacz [Rejestrowanie](xref:core/miscellaneous/logging).</span><span class="sxs-lookup"><span data-stu-id="cd687-154">For more information on viewing logging output, see [Logging](xref:core/miscellaneous/logging).</span></span>

<span data-ttu-id="cd687-155">Opcjonalnie EF Core pozwoliło na zmianę domyślnego zachowania albo zgłosić wyjątek lub nic nie robić podczas wykonywania oceny klienta (z wyjątkiem w projekcji).</span><span class="sxs-lookup"><span data-stu-id="cd687-155">Optionally EF Core allowed you to change the default behavior to either throw an exception or do nothing when doing client evaluation (except for in the projection).</span></span> <span data-ttu-id="cd687-156">Zachowanie zgłaszania wyjątków sprawi, że będzie podobny do zachowania w 3.0.</span><span class="sxs-lookup"><span data-stu-id="cd687-156">The exception throwing behavior would make it similar to the behavior in 3.0.</span></span> <span data-ttu-id="cd687-157">Aby zmienić zachowanie, należy skonfigurować ostrzeżenia podczas konfigurowania opcji kontekstu — zazwyczaj w `DbContext.OnConfiguring`, lub w `Startup.cs` jeśli używasz ASP.NET Core.</span><span class="sxs-lookup"><span data-stu-id="cd687-157">To change the behavior, you need to configure warnings while setting up the options for your context - typically in `DbContext.OnConfiguring`, or in `Startup.cs` if you're using ASP.NET Core.</span></span>

```csharp
protected override void OnConfiguring(DbContextOptionsBuilder optionsBuilder)
{
    optionsBuilder
        .UseSqlServer(@"Server=(localdb)\mssqllocaldb;Database=EFQuerying;Trusted_Connection=True;")
        .ConfigureWarnings(warnings => warnings.Throw(RelationalEventId.QueryClientEvaluationWarning));
}
```
